# CLAUDE.md

이 저장소에서 작업할 때 참고할 것. 파이프라인 자체의 동작 방식(모듈 구조,
브랜드/카탈로그 개념)은 `README.md`에 정리돼 있으니 먼저 그걸 읽을 것 —
여기는 README에 없는, 여러 번의 시행착오로 얻은 규칙과 주의사항만 적는다.

## 실행 중인 서비스

- **웹 대시보드**: https://openmarket-order-app.onrender.com/ (Render.com,
  `render.yaml` 기준 Blueprint 배포). `claude/openmarket-order-excel-65ayin`
  브랜치(=이 저장소의 기본 브랜치)에 푸시하면 자동 배포된다.
- 영구 디스크(`APP_DATA_DIR=/var/data`)에 실제 운영 데이터가 쌓인다.
  저장소에 커밋된 `data/`는 최초 부팅 시 디스크가 비어 있을 때만 한 번
  복사되고(`src/paths.py`의 `seed_if_empty()`), 그 뒤로는 서로 무관하게
  따로 움직인다 — **저장소의 `data/orders/*.json`을 고쳐도 라이브 서버에는
  반영되지 않는다.** 라이브 데이터를 지우거나 고치려면 그 화면(처리
  이력의 "삭제" 버튼 등)을 실제로 써야 한다. Claude는 라이브 서버 디스크에
  직접 접근할 방법이 없다.

## 절대 어기면 안 되는 테스트 규칙

- **로컬에서 웹앱을 실행/테스트할 때는 항상 `APP_DATA_DIR`를 저장소 밖의
  임시 디렉터리로 지정할 것.** `APP_DATA_DIR` 없이 `webapp.app`을
  import/실행하면 저장소의 실제 커밋된 `data/`를 건드려서 재현 불가능한
  실 데이터 손상이 난다(이 프로젝트에서 실제로 두 번 일어났던 사고).
  ```bash
  export APP_DATA_DIR=/tmp/.../fresh_dir   # 매 테스트마다 새 디렉터리
  python3 -m uvicorn webapp.app:app --host 127.0.0.1 --port <포트>
  ```
- 커밋 전에는 항상 `git status --short`로 `data/orders/`, `data/reference/`
  등 실제 데이터 파일이 의도치 않게 바뀌지 않았는지 확인한다.
- `.gitignore`: `data/users.json`, `data/.session_secret`, `data/board.json`,
  `data/runs.json`은 런타임 전용이라 추적하지 않는다. `data/orders/*.json`과
  `data/reference/*.json`은 실제로 정리해 온 데이터라 git으로 추적한다.

## 웹앱 구조에서 특히 주의할 것

- **미들웨어 등록 순서**: `@app.middleware("http")`로 등록한 미들웨어는
  나중에 등록할수록 더 바깥쪽(먼저 실행)이 된다. 로그인 검사 미들웨어를
  `SessionMiddleware`보다 먼저 등록해야 한다(`webapp/app.py` 상단 주석 참고).
- **"실행(run)"과 "날짜"는 다른 개념** (`src/runs.py`): 하루에 여러 번
  나눠 올리면 실행은 여러 건, 날짜는 하나. 처리 이력 화면의 통계는
  실행 단위, 발주 파일/대시보드/확인필요 목록은 그날 전체 합산 기준.
- **업로더별 접근 제어**: 일반 직원은 본인이 올린 실행만 보고(요약/발주
  파일/확인필요 목록 모두 그 실행분에 한정해서 그 자리에서 생성),
  관리자는 전체 + 누가 올렸는지 본다. 그날 전체 합산 발주 파일/대시보드는
  관리자만 볼 수 있다(여러 사람 내용이 섞이므로).
- **실행 삭제** (`POST /runs/{run_id}/delete`): 본인 또는 관리자만 가능
  (게시판 글 삭제와 같은 권한 모델). 삭제하면 그 실행의 보관 라인을
  지우고, 그날 남은 라인만으로 `main.rebuild_day_outputs()`를 다시 돌려
  발주 파일/확인필요 목록/대시보드를 재생성한다(지운 실행 내용이 다운로드
  파일에 유령처럼 남지 않게). 중복 방지 키가 `(_source_file, _source_row)`
  라서, 사업부를 잘못 선택해 처리한 걸 바로잡으려면(같은 파일을 다시
  올려 재처리) 먼저 이 삭제를 해야 한다 — 지우지 않으면 재업로드해도
  중복으로 걸려 아무 내용도 새로 반영되지 않는다.
- **업로드 실패는 반드시 화면에 이유를 보여줄 것**: 예전에 처리 중
  예외가 나면 빈 500 에러만 뜨고 아무것도 저장되지 않아 "업로드가 안
  된다"는 혼란스러운 버그 리포트로 이어진 적이 있다. `/process`는
  실패 시 무조건 업로드 화면으로 돌아가 `.note.error`로 원인을 보여준다
  (`webapp/app.py`의 `_upload_error`).

## "상품 일괄등록" 같은 참고자료 정리 요청을 받으면

사용자가 종종 오픈마켓 상품현황/재고현황 엑셀을 주고 "우리 시스템으로
분류해서 양식에 채워달라"고 요청한다. 아래는 여러 번 수정 지시를 받으며
확정된 규칙이다 — 처음부터 이대로 하면 된다:

1. **템플릿 컬럼 의미** ("상품 일괄등록 샘플" 류 xls, 보통 "v10_pei"):
   - 1열 `상품명` = **우리 엔진이 실제 발주 파일을 만들 때 산출하는
     "정식 옵션명"** (`src/writer.py`의 `write_vendor_file` 내부 파이프라인
     — `_normalize_x_multiplier` → `_expand_box_count_bundle` →
     `_collapse_repeated_unit` → `_expand_multi_select` →
     `_expand_variety_set` → `_expand_plus_combo` → `_apply_option_recalc`
     순서로 그대로 재현해야 한다). **카탈로그의 `managed_name`을 그냥
     넣는 게 아니다** — managed_name은 정식 옵션 매칭이 실패했을 때만
     쓰는 차선책이다.
   - 2열 `공급처코드 / 공급처명` = 보통 `{협력사명}{사업부}` 붙여쓰기
     (예: `이플코리아MH`). **서울문화사는 예외**: `code_lookup_file`
     (`data/reference/codes_서울문화사.json`)로 찾은 코드를
     `서울문화사/CR022`처럼 슬래시로 붙인다(`src/writer.py`의
     `_code_for`).
   - 3~4열 `공급처 상품명`/`공급처 옵션` = 오픈마켓에 노출되는 원본
     상품명/옵션 그대로(가공하지 않음).
   - 5~10열(원산지/택배비/중량/원가/공급가/판매가)은 확실한 값이 없으면
     비워둔다 — **특히 원가/단가를 추측해서 채우지 말 것.**
2. **사업부(브랜드)를 모르는 상품**은 `src/catalog.py`의
   `lookup_vendor(brand, ...)`를 JM/MSNA/MH 세 번 다 시도해서 판단한다.
   이플코리아 전역 우선배정(`_ep_korea_override_vendor`)은 브랜드 없이도
   걸리지만 그 경우 사업부는 특정할 수 없다(공급처명에 사업부 접미사
   없이 "이플코리아"만 표기).
3. **확신 등급을 색으로 구분해달라는 요청을 받으면**: 확정(흰 배경) /
   실험적 추정(연한 cyan, `#E0FFFF`) / 계산 불가(빈칸, 색 없음) 3단계를
   쓴다. 정식 옵션 매칭에 성공하면 확정, 공급처는 알지만 정식 옵션명을
   못 정했으면(카탈로그의 managed_name 후보 중 마켓 원문과 토큰이 가장
   겹치는 걸 추정) 실험적, 공급처 자체를 못 찾거나 여러 사업부에서 서로
   다른 공급처로 갈리면(진짜 애매함) 빈칸으로 둔다. **정식 옵션명 결과가
   빈 문자열인데 "확정"으로 표시하는 실수를 주의할 것** — 협력사에
   정식 옵션 목록 자체가 없어서 원문을 그대로 통과시키는 경우, 결과가
   빈 값이면 마켓 상품명 등으로 대체하고 반드시 실험적으로 낮춰야 한다.
4. **콤보/묶음 상품이 여러 정식 옵션으로 쪼개지는 경우**(실제 주문
   처리와 동일한 로직으로 한 줄이 여러 줄이 됨), 오픈마켓/공급처명/원본
   상품명/원본 옵션 등 공통 컬럼은 셀 병합(`openpyxl.merge_cells`)하고
   `상품명`(정식 옵션, 줄마다 다름) 컬럼만 따로 둔다. 병합된 묶음은
   왼쪽에 굵은 테두리 등을 넣어 옆의 무관한 항목과 구분되게 한다.
5. **여러 오픈마켓 export 파일을 한 번에 받으면**: 마켓마다 컬럼 구조가
   완전히 다르다(옵션 텍스트가 아예 없는 마켓도 많음 — 롯데ON/스마트
   스토어/올웨이즈/지그재그/카카오선물/카카오쇼핑; 이 경우 상품명만으로
   매칭). 결과 앞에 "오픈마켓" 컬럼을 추가해 출처를 표시해달라는 요청을
   받은 적이 있다.
6. **오픈마켓 상품현황/재고현황처럼 실제 고객 "주문"이 아닌 목록성
   데이터를 카탈로그 매칭에 태울 때는, 정식 옵션 문구가 실제 주문 문구와
   달라(관리자 화면 특유의 표기, 옵션그룹 라벨 등) 매칭 성공률이 많이
   떨어질 수 있다.** 이건 버그가 아니라 두 데이터 소스의 문구 관례가
   다른 데서 오는 본질적 한계이니, 실행 결과를 보고할 때 이 점을 분명히
   설명할 것.

## 이 사용자와 일할 때의 일반적 습관

- 마케팅/운영 실무자다. 요청은 보통 구체적인 실제 파일(엑셀)을 첨부해서
  오고, 결과도 엑셀로 돌려주길 기대한다.
- 파일 업로드/다운로드 관련 버그 리포트를 받으면, 먼저 로컬에서 실제
  파일로 재현을 시도할 것(추측성 수정 금지). 재현 안 되면 사용자가 준
  화면 문구/파일을 근거로 정확한 원인을 짚고, 직접 서버에 접근할 수
  없다는 한계를 솔직히 말할 것.
- 내가 임의로 값을 채우거나 가정하면 바로 지적받는다("네 멋대로
  채워넣지 말아줘") — 확신이 없으면 빈칸으로 두거나(또는 요청에 따라
  실험적 표시), 무엇을 모르는지 명확히 알릴 것.
- 실제 협력사/공급처 데이터, 원가 등은 민감한 사업 데이터이니 추측으로
  단정하지 말 것.
